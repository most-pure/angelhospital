/* import { parseISO, format } from 'date-fns'

export default function Date({dateString}: any) {
    const date = parseISO(dateString )
  return (
    <div>
      <time dateTime={dateString}>{format(date, 'LLL d, yyyy hh:mm a' )}</time> 
    </div>
  )
} */
